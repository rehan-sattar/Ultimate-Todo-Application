// import todos from "./todos";
// import ultimateTodoReducer from "../store/Reducer";

// test('should setup the default values', () => {
//     const state = ultimateTodoReducer(undefined, { type: '@@INIT' });
//     expect(state).toEqual({
//         allTodos: [],
//         error: ''
//     });
// });


// test('should add one to the end of todos' , () => {
//     const action = {
//         type : 'ADD_TODO_SUCCESS',
//         payload : '4'
//     };

//     expect(ultimateTodoReducer(todos, action.type)).toEqual( todos[0], todos[1], todos[2],  {addedTodo : true })
// })
